{
  "name": "gb-server",
  "module": "src/index.ts",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "build": "./scripts/build.sh",
    "deploy": "./scripts/deploy.sh",
    "test": "./scripts/test.sh",
    "clean": "rm -rf .aws-sam",
    "dev": "npm run build && npm run test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/uuid": "^11.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "mongodb": "^6.20.0",
    "uuid": "^9.0.1"
  }
}
